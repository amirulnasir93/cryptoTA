# Copy to .env.local (gitignored by Vite's default .gitignore) if you need to
# point the frontend at a backend that isn't http://localhost:3001 — e.g. once
# it's hosted somewhere. See ../../docs/DEPLOY.md.
VITE_API_URL=http://localhost:3001
